name: ble_data_transfer_demo
description: A demo for the BLE data transfer library.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  ble_data_transfer:
    path: ../ble_data_transfer/

  cupertino_icons: ^1.0.2
  flutter_blue: ^0.8.0
  permission_handler: ^8.3.0
  logger: ^1.1.0
  provider: ^6.0.1
  path_provider: ^2.0.8
  http: ^0.13.4
  intl: ^0.17.0
  connectivity_plus: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

